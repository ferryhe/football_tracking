# Production Workflow Final Managed PR Plan

> **For agentic workers:** implementation must use the managed-pr-development workflow. Every PR starts from the latest clean main branch, uses a dedicated implementation agent, receives separate specification and code-quality reviews, passes focused and required validation, waits for remote GitHub feedback, and merges only after all valid feedback is resolved.

**Goal:** replace the current collection of parallel Baseline, Broadcast, Deliverable, AI, and History entry points with one sequential production workspace and one grouped production-history workspace, preserve existing backend contracts and production artifacts, and add the approved fail-closed review-evidence provisioning contract needed by the real production gate.

**Architecture:** the frontend owns a versioned production draft and derives the current user step from the draft plus authoritative backend run state. Existing APIs remain authoritative for inputs, asset groups, configurations, runs, broadcast review, recomputation, rendering, artifacts, and quality reports. Field calibration is edited in source-frame pixel coordinates through a React Konva overlay. Configuration confirmation creates a uniquely named, operator-locked configuration snapshot with a content digest and persisted workflow lineage. Because the current configuration API permits update, delete, and same-name overwrite, this is a detectable logical lock rather than a server-enforced immutable object.

**Primary stack:** React 19, TypeScript, Wouter, TanStack Query, React Konva, generated API client, FastAPI backend, existing broadcast workflow helpers, Playwright browser tests, frontend unit/component tests, and the existing Python verification suite.

---

## Canonical Plan Status

This document is the canonical development plan for the production workflow, review-evidence provisioning, and grouped history consolidation.

It covers:

1. The final user-facing experience.
2. The eight-PR delivery sequence, including the approved review-evidence provisioning and target-audit/configuration-lineage dependencies.
3. Per-PR acceptance criteria.
4. Updated automated test and quality gates.
5. Migration, rollout, rollback, and managed-PR execution rules.

Implementation must not begin from an ambiguous workspace. At the time this plan was written, main matched origin/main but pnpm-workspace.yaml had an unrelated local modification. That change must be identified, preserved, and isolated before PR1 starts.

## Program Execution Status

**Program status:** In progress  
**Current PR:** PR5 · Grouped Production History
**Heartbeat:** ACTIVE · production-workflow-multi-pr-heartbeat · every 15 minutes  
**Last updated:** 2026-07-17 00:18 ET

The previously observed pnpm-workspace.yaml placeholder was generated by pnpm, contained no approved boolean decision, and has been removed from the working tree. The tracked baseline now matches origin/main.

| PR | Status | Branch | Pull request | Checks and review |
| --- | --- | --- | --- | --- |
| PR1 · Production Workflow Foundation | Completed | codex/production-workflow-foundation | [#104](https://github.com/ferryhe/football_tracking/pull/104) | Merged 2026-07-15T10:43:22Z as ea52548; Node/Python CI passed; 1 valid Copilot comment and 1 valid CI failure fixed and independently re-reviewed |
| PR2 · Interactive Field Calibration | Completed | codex/interactive-field-calibration | [#105](https://github.com/ferryhe/football_tracking/pull/105) | Merged 2026-07-15T12:29:09Z as c781b34; two full feedback windows, Node/Python CI green, 1 valid Copilot aria-live comment fixed and resolved |
| PR3 · Trial, Tuning, and Configuration Freeze | Completed | codex/trial-tuning-and-config-freeze | [#106](https://github.com/ferryhe/football_tracking/pull/106) | Merged 2026-07-15T15:46:13Z as 1dd538c after a full feedback window; Node/Python CI passed and Copilot reviewed 17/17 files with no comments |
| PR4A · Review Evidence Provisioning | Completed | codex/review-evidence-provisioning | [#107](https://github.com/ferryhe/football_tracking/pull/107) | Merged 2026-07-16T04:46:51Z as `3546280` after two complete feedback windows. Node/Python CI passed; one valid Copilot dataset-binding concern and both CI failures were fixed and independently re-reviewed; the Copilot thread was replied to and resolved. Official Python passed 1,510 tests with 11 platform skips; the native separated Dev/Qual evidence chain, web 322, Chromium 24, generated-client idempotence, TypeScript, builds, Ruff, OpenAPI, workspace/lock, cleanup, and mobile visual gates passed. |
| PR4B · Target Audit and Configuration-Lineage Reconfirmation | Completed · specification COMPLIANT · quality APPROVE | codex/target-audit-config-lineage | [#108](https://github.com/ferryhe/football_tracking/pull/108) · `c3cc780` | Merged 2026-07-16T17:32:36Z as `3ee9091` after the final 2026-07-16T17:22:20.107Z–17:32:20.107Z feedback window. GitHub Actions run 29519492616 passed Node and Linux Python. Two valid Copilot comments and three successive Linux findings were fixed, independently re-reviewed, replied to where applicable, and resolved; final thread-aware audit found no valid or ambiguous feedback. |
| PR4 · Full Production, Review, and Render | Completed · specification COMPLIANT · quality APPROVE | codex/full-production-review-render | [#109](https://github.com/ferryhe/football_tracking/pull/109) · `e1870ca` | Merged 2026-07-17T01:24:48Z as `6e22866`. The final 2026-07-17T01:12:13.411Z–01:22:13.411Z feedback window completed, GitHub Actions run 29546835802 passed Node and Linux Python, and the final REST plus thread-aware audit found no valid or ambiguous feedback. The two Copilot submissions were identical external reviewer-service errors after the required single retry and contained no code comments. The POSIX lock fix and Linux contract follow-up passed independent specification and quality/security reviews plus the official 1,664-test local Python route. The engineering merge is complete; real evidence and independent visual acceptance remain fail-closed operational release gates. |
| PR5 · Grouped Production History | PR #110 remote correction complete locally · final specification COMPLIANT · final quality/security APPROVE · correction push pending | codex/grouped-production-history | [#110](https://github.com/ferryhe/football_tracking/pull/110) · remote head `2294843` | The initial Node/Python run passed, but Copilot raised valid thread 3600212785 on raw path variants. The correction now preserves canonical separator/trailing-slash and Windows drive-root identities, deterministically deduplicates source/config/run records, and fails closed on conflicting identity evidence. Fresh validation passed Vitest 483/483 with overall 91.52% branch coverage and `productionHistory.ts` at 91.04%, app/test TypeScript checks, history E2E 2/2, Chromium 36/36, formatting, and diff integrity; prior build, workflow/component, axe, console, boundary, and rollback checks remain applicable. Final independent reviews are COMPLIANT and APPROVE. Next: push once, resolve the thread, restart the full 10-minute window, require corrected-head Node/Python green and a clear thread-aware audit, then merge. |
| PR6 · Cutover, Compatibility, and Release Hardening | Pending | codex/production-workflow-cutover | Pending | Waiting for PR5 |

### Engineering Merge vs. Operational Release Boundary (approved 2026-07-16)

- PR4's engineering merge gate consists of the managed-PR requirements: current main integration, independent specification and quality review, required local validation, GitHub CI, a complete 10-minute remote-feedback window after the latest material push, and resolution of every valid or ambiguous remote finding.
- Missing real labels, a trusted multi-person reviewer team, a qualified real target bundle/queue, configuration-lineage publication, and independent visual activation audit do not block merging the fail-closed implementation after the engineering gate passes.
- Those missing resources remain an operational release gate. The real run may be used for development, bounded trials, diagnostics, and evidence preparation, but it cannot enter production-ready/render or be represented as independently audited or production-validated until the existing runtime gates pass.
- The present code remains fail-closed. This approval does not authorize an empty queue, default acceptance, fabricated labels, identity fabrication, same-person claims of independence, or weakening any model/policy/evidence threshold.
- A future feature that emits a separately labeled `not_independently_reviewed` deliverable would require explicit product design, implementation, and tests; it is not silently inferred by this governance change.
- PR5 and PR6 may proceed from merged engineering baselines. Their release checklist must continue to carry the unresolved real-evidence and independent-visual-audit items.

### PR4 Real-Video Gate Dependency and Approval (2026-07-15)

- Source: `python_backend/data/raw5760x144020fps.mp4`, reported 5,194 frames at 20 fps.
- Accepted trial: `production_trial_e3647dbc-59ec-400a-84f1-b19590086a41`.
- Locked configuration: `generated/production_841ebbec-3d7e-4f65-85dd-532f2a642bb5_1039c7da-c81d-43cf-8889-e1f13f19df67.yaml`, SHA-256 `6fd624d76b2688982f3fd18922d6aaf16d519f3da598b653f44d62da9a5fda4c`.
- Failed diagnostic full run: `production_full_d05eebc5-6d4a-4a56-8211-472e333bb4c8`; it proved the HEVC source has a corrupt two-frame terminal tail.
- Validated retry full run: `production_full_0276b82e-3dc5-445e-8758-e2de527ea216`; all 5,192 decodable frames completed and the authoritative state is `needs_review` with the permanent `action_signal_terminal_decoder_shortfall` limitation.
- The hash-bound terminal-tail acknowledgement is fail-closed, immutable, visible in the Production workspace, independently reviewed SAFE, and does not remove any unrelated blocker.
- The real acknowledgement was recorded at `2026-07-15T21:06:05.781093+00:00` by reviewer `operator`; evidence SHA-256 `fa331352742f84a0fc1831abfeb26040df3fa7ff1db45f366792119107c55c4b`, acknowledgement SHA-256 `b964b2ef9a3c22d4660222642731ed9b5331671663c1028e284c0ac6152ad01e`. The run remains `needs_review` for the missing qualified selective review evidence.
- The tracking contract contains 6,406 candidates and zero classifications, decisions, prelabels, or confirmed labels. No qualified model manifest, training report, selective policy, dataset manifest, or review queue exists in the repository or runtime workspace.
- PR4A merged on 2026-07-16 and now supplies an additive, fail-closed import/qualification contract plus offline annotation/training protocol. It does not itself supply target labels, independent provenance groups, a trusted auditor team, or a qualified target bundle.
- An independent post-merge operational-resume audit returned `PAUSED`: no target qualification package or queue exists, only one logical match group is available, the confirmed `6fd624...da4c` configuration digest has no matching immutable snapshot in the inspected run/config outputs, and no independent visual activation audit can be performed. The immediate safe sequence for production activation is configuration-lineage recovery, policy-statistics feasibility review, acquisition of genuinely independent matches and reviewer roles, then frozen model-development/policy-qualification/target-application evidence; only a fully reconciled and independently audited activated queue can resume the real run toward production-ready/render. This does not block merging fail-closed engineering code under the separately approved boundary above.
- Follow-up lineage analysis found the current YAML's LF-normalized text hashes exactly to `6fd624...da4c`; the apparent `c203ae...69cc` mismatch is the CRLF raw-byte hash. Treat this as a canonicalization and immutable-snapshot provenance defect, not evidence that configuration values changed. Any recovery must preserve the old run and both digests and must not rewrite history.
- Follow-up policy analysis returned `INFEASIBLE_CURRENT_CONTRACT`: because connected-component thinning admits only one evaluation candidate per independent match/source group, the exact zero-error bounds require hundreds to more than one thousand independent policy groups (approximately 738–1,151 with directed role allocation, or 791–1,656 under naturally balanced true-ball roles). Do not begin large-scale annotation under this contract. A policy-statistics defect fix or exhaustive-human mode requires separate approval before the evidence subproject proceeds.
- The user approved Option A on 2026-07-16: a combined PR4B adds `qualification_scope=target_finite_population` without changing the existing global policy contract and implements the append-only configuration-lineage reconfirmation. It freezes the model, thresholds, 6,406-candidate target population, configuration, and sampling design before labels are opened; samples without replacement; prohibits target labels from training, calibration, or threshold selection; and binds qualification to the exact run/population/model/config/policy digests. PR4B is now part of the eight-PR managed sequence.
- A configuration-lineage recovery audit proves that the observed 8,292-byte CRLF YAML with raw SHA `c203ae...69cc` converts through exactly 286 CRLF-to-LF replacements into an 8,006-byte canonical text whose SHA is the confirmed `6fd624...da4c`. This proves unambiguous same text content, not that a historical raw LF snapshot existed. Recovery therefore requires a new append-only, independently reviewed reconfirmation generation and must not rewrite either old full-run record, notes, run output, the current CRLF file, or either digest.
- PR4 cannot honestly reach recompute, render, or `ready` by synthesizing an empty queue, accepting all candidates, or fabricating classifier truth.
- The user approved a separate Review Evidence Provisioning PR plus the necessary annotation/training subproject on 2026-07-15. PR4A must provide qualified, reproducible, human-grounded evidence before the real target run resumes toward production-ready/render; the approval does not authorize default acceptance, fabricated labels, leakage-prone evaluation, or unqualified models.

### Approved PR4B Decision Boundary (Option A · 2026-07-16)

- Keep globally qualified v1 policies and their existing inferential-unit rules unchanged. Add an explicit target-only schema/scope discriminator; old consumers must reject the new scope safely, and no old artifact is reinterpreted or migrated.
- Use an exact finite-population hypergeometric upper bound for the frozen policy-accepted population, with at least 183 zero-error audited accepts as the conservative binomial reconciliation floor. For false rejects, use the conservative binomial zero-error bound of at least 368 audited human-confirmed true balls unless an independently reviewed finite-population ratio-confidence method is specified and tested. Any insufficient support or failed bound remains `review_only`.
- At an assumed 20–60% target true-ball prevalence, 614–1,840 reviewed candidates, or approximately 1,228–3,680 blind primary votes before adjudication, is only an expected-value planning floor. Before labels are opened, the protocol must predeclare a larger sample using a conservative lower prevalence bound or a label-independent staged design. Qualification remains `review_only` unless the frozen audit obtains both at least 183 audited accepted candidates and at least 368 audited true balls. One observed error materially increases support requirements; near 10% prevalence approaches the exhaustive-human route.
- Require deterministic hash-bound sampling, label-independent ordering, complete blind labels, exact population coverage reconciliation, and fail-closed rejection of duplicate candidates, partial labels, adaptive resampling, target-label leakage, or reuse across another run, population, model, configuration, or policy digest.
- PR4B completed and merged as `3ee9091`. Refresh `codex/full-production-review-render` from that latest `main`, resolve integration, and rerun PR4's independent specification review, quality review, and required validation before PR4 engineering publication can resume. That refresh and validation are now complete. The ledger, declared total, and heartbeat scope remain eight PRs.
- The alternative exhaustive-human route requires at least 12,812 blind primary votes for all 6,406 candidate boxes and is operationally simpler, but neither route proves missed-ball safety or resolves the separate trajectory gaps, suspicious tracklets, or large jumps.

#### Configuration-Lineage Reconfirmation Boundary

- Option A is the approved governance boundary: target audit and configuration-lineage reconfirmation are combined in PR4B, making eight total PRs. The discarded split and exhaustive alternatives do not authorize PR4C or any additional code PR.
- Build a new service-managed generation under `python_backend/outputs/config_lineage_reconfirmations/<target_run_id>/generations/`. It contains only `confirmed_config.canonical-lf.yaml` and a versioned manifest. Under a target-run cross-process lock, exclusively create a randomly named no-follow staging directory as an immediate child of the trusted same-volume `generations` directory, fsync both files and the staging directory, publish with atomic no-replace semantics, then fsync the parent directory. If the destination already exists, perform handle-based no-follow revalidation and accept only a complete byte-identical idempotent replay; every differing or additional generation fails closed, and nothing is overwritten, deleted, or repaired in place.
- Bind the manifest to target run, confirmed configuration name/text digest, observed raw path/digest/size/file identity/newline counts, canonical snapshot path/digest/size/UTF-8-LF-no-BOM rules, accepted trial, request, intent, patch, calibration, source signature, both historical full-run/submission identities, notes digests, operator, and independent reviewer. Record both `same_text_content_reconfirmed` and `historical_raw_snapshot_not_observed`.
- Traverse component by component, handle-relative, beneath pre-opened trusted roots. Reject symlink, junction, reparse, FIFO/device, alias, hard-link, or other special nodes from the opened handles. Compute raw and canonical hashes during one read of one handle, compare identity and size before and after on that same handle, and never validate a pathname and then reopen it unchecked. Apply the same traversal, identity, and no-follow rules to every manifest and snapshot use; reject mixed newlines, BOM, invalid UTF-8, identity changes, and size changes. Read-only attributes are not a security boundary.
- Add an append-only child operation/projection that displays `confirmed_text_sha256`, `observed_raw_sha256`, `canonical_snapshot_sha256`, generation ID, and `historical_raw_snapshot_observed=false`. Do not modify either historical full run, registry record, notes, run manifest, output, the observed CRLF configuration, or the previous inventory conclusion; add a superseding correction.
- Evidence discovery/import may use the generation only when every target, name, digest, deterministic conversion, workflow, accepted-trial, patch, calibration, source, reviewer, inventory, and generation binding validates and no conflicting generation exists. Add stable fail-closed blockers for required reconfirmation, unsafe snapshot, mismatch, and conflicting generations.
- Successful reconfirmation removes only the configuration-lineage blocker. Missing human-confirmed corpus, insufficient independent evidence, infeasible policy, missing qualified bundle/queue, trajectory gaps, suspicious tracklets, large jumps, and the independent visual audit remain separate gates.

## Product Decision

The application has two primary user-facing workspaces.

### 1. Production Workspace

The operator starts from the original video and moves down one step at a time. The top of the page shows only a lightweight current-step indicator such as:

> Step 2 of 5 · Field calibration

The five user-facing stages are:

1. Original video
2. Field calibration
3. Trial and tuning
4. Full tracking and review
5. Final product

The five stages are not five separate tabs or five independently clickable cards. Only the current step is expanded. Completed steps collapse to summaries and may be reopened. Future steps are hidden or disabled until their prerequisites are satisfied.

Internal workflow states may be more detailed than the five user-facing stages:

- source
- calibration
- trial
- config_confirmation
- full_tracking
- review
- recomputing
- trajectory_ready
- rendering
- ready
- failed
- cancelled

### 2. Production History

History is projected from backend AssetGroups for the same original video. It is not a flat list of run identifiers. A normal group is keyed in the client by input_video.path; the current slug-derived group_id is a display/URL alias and must not be assumed globally unique. The unbound group uses the fixed unbound-legacy key.

Each group provides:

- Original-video identity and metadata
- Current activity and latest status
- Trial attempts
- Confirmed configuration snapshots
- Full tracking runs
- Manual-review and recomputation operations
- Render operations
- Failed and cancelled attempts
- Final product versions

A run is a final broadcast product only when:

1. The authoritative broadcast state is ready.
2. A verified broadcast.mp4 artifact is available.

The existing Follow Cam heuristic must not be used to classify a broadcast product.

## User Workflow

1. Select the original video.
2. Inspect source metadata and preview frames.
3. Draw or edit the field polygon on the image.
4. Verify the same approved polygon on three distinct source frames.
5. Start a bounded trial run.
6. Inspect the result and adjust parameters.
7. Repeat the trial until the operator accepts the result.
8. Derive and operator-lock a uniquely named, digest-verified configuration snapshot.
9. Start full tracking from the locked configuration.
10. Review uncertain candidates only when the backend requires review.
11. Recompute the reviewed trajectory when required.
12. Render and validate the broadcast product.
13. Preview and download the product in the production workspace.
14. Reopen the complete lineage later from grouped production history.

## Final Deliverables

### User-Facing Deliverables

- A production workspace at /production.
- A production-history workspace at /history.
- A single-current-step production experience with Back, Save and exit, and Next actions.
- Source selection and versioned draft recovery.
- Interactive field-polygon drawing, dragging, deletion, undo, clear, and coordinate display.
- Three-frame calibration verification inside field calibration.
- Trial execution, monitoring, cancellation, result inspection, tuning, and rerun loop.
- Explicit configuration confirmation and a uniquely named, digest-verified configuration snapshot.
- Full broadcast tracking, conditional manual review, recomputation, rendering, quality reporting, and delivery.
- Final product preview and download in both the completed workspace and history detail.
- Legacy URL compatibility during migration.
- Chinese and English user-facing copy.

### Engineering Deliverables

- A pure production-workflow state model with transition guards.
- A versioned frontend draft schema and migration behavior.
- Pure coordinate and polygon geometry utilities.
- A production adapter over existing baseline and broadcast request builders.
- AssetGroup-based history projection and product classification.
- Automated unit, component, browser, accessibility, and visual tests.
- Updated CI required checks.
- Operator documentation, test documentation, release evidence, and rollback instructions.
- A managed-PR ledger containing PR URLs, branches, commits, checks, review decisions, merge status, and cleanup status.

## Explicit Non-Goals

- No tracking-algorithm redesign.
- No model training or detector-quality work outside the separately approved PR4A candidate-classifier evidence subproject; detector redesign remains out of scope.
- No new backend API schema except the approved PR4A evidence-provisioning contract and the approved PR4B target-audit/configuration-lineage contract prompted by documented real-video blockers.
- No cross-device or multi-user draft synchronization in the first release.
- No silent deletion of legacy AI or highlight capabilities.
- No broad UI rewrite outside the production and history consolidation.
- No replacement of deterministic backend quality gates with frontend heuristics.

## Authoritative Data and Invariants

### Original Video and Grouping

- Production history consumes backend AssetGroups but uses input_video.path as the normal client group key.
- The slug-derived AssetGroup group_id may be used as a URL/display alias only after collision detection.
- The UI must not group only by display filename or assume group_id is globally unique.
- Unbound legacy runs appear in an explicit Unarchived records group.
- A production draft binds the original video signature: path, size_bytes, and modified_at.
- If any source-signature field changes, calibration and all downstream state are invalidated with an operator-visible explanation.

### Calibration

- Approved polygon points are stored in source-frame pixel coordinates.
- Points must be finite and inside the source-frame boundary.
- A polygon requires at least three points and non-zero area.
- Display-to-source-to-display round trips must stay within one display pixel.
- The UI must not silently clamp invalid coordinates.
- Pointer dragging may be visibly constrained to the canvas boundary; typed or restored out-of-range coordinates must raise validation errors rather than being silently corrected.
- Changing the approved polygon invalidates all three frame confirmations.
- Changing the source video invalidates calibration and all downstream work.
- A frame may be confirmed only when its preview is loaded, a valid polygon is visible, and the confirmation records the frame identity plus polygon digest.
- AI, configuration, detection, and fallback suggestions are advisory seeds only.
- Suggestion source and confidence must be visible to the operator.

### Trial and Configuration

- Trial requests use the approved polygon and exclusions.
- Failed or cancelled trials cannot be accepted as the configuration basis.
- A completed trial is still advisory until required result artifacts and quality signals are readable and the operator explicitly accepts it.
- Retrying creates a new run record whose parent_run_id points to the preceding trial in the same production chain.
- Configuration confirmation derives a UUID-named persisted configuration snapshot and stores its SHA-256 in the draft.
- The configuration metadata.production_workflow record contains schema version, workflow ID, base configuration, accepted trial run ID, calibration digest, source signature, and confirmation time.
- The full broadcast run parent_run_id points to the accepted trial run; recompute and render operations continue to point to the full broadcast parent.
- The full run notes persist a machine-readable workflow ID and expected configuration SHA-256 so history can detect later configuration modification or deletion.
- The full run uses the confirmed snapshot; it cannot use mutable unconfirmed form state.
- The locked snapshot must not retain trial-only start_frame, max_frames, temporary output, or legacy render settings.
- Editing source, calibration, or tuning after confirmation requires explicit invalidation.

### Broadcast and Product

- Backend run and broadcast status remain authoritative.
- Before starting trial, full tracking, recompute, or render, the UI checks for the backend's single active run, displays the occupying task, and handles the server race/conflict response.
- Refreshing the page must not create a duplicate run.
- Refreshing an active run resumes monitoring; cancelling or failing a run never resumes the same computation and instead requires an explicit new retry run.
- Manual review appears only when the backend reports review work.
- A ready status without a verified broadcast.mp4 is a blocked delivery, not a product.
- AssetGroup/list-runs payloads intentionally omit artifacts for ready broadcasts. Product verification is lazy on group expansion/detail through the dedicated artifacts endpoint and cached by run_id plus broadcast.status_generation.
- Only verified artifact URLs are rendered as product downloads.
- Operation children remain linked to the correct parent run.

---

## Delivery Summary

| PR | Branch | Main Outcome | Estimated Effort |
| --- | --- | --- | --- |
| PR1 | codex/production-workflow-foundation | State model, source-bound draft persistence, workspace shell, and CI/test foundation | 2–3 days |
| PR2 | codex/interactive-field-calibration | React Konva polygon editor and integrated three-frame verification | 2–3 days |
| PR3 | codex/trial-tuning-and-config-freeze | Trial/tuning loop, persistent lineage, and digest-verified configuration confirmation | 2.5–3 days |
| PR4A | codex/review-evidence-provisioning | Self-contained qualified-evidence bundle contract, managed import/activation, UI state, and offline annotation/training protocol | 3–5 days plus external evidence runtime |
| PR4 | codex/full-production-review-render | Full tracking, review, recomputation, render, recovery, and completed product | 3–3.5 days |
| PR5 | codex/grouped-production-history | AssetGroup history, lazy product verification, lineage, and run operations | 2.5–3 days |
| PR6 | codex/production-workflow-cutover | Two-entry navigation, compatibility, quality hardening, and release | 2–2.5 days |

The expected single-engineer total is approximately 17–23 engineering days, excluding real full-video runtime, human annotation volume, model-training runtime, and external review turnaround.

## Milestones

### Milestone A: Internal Alpha

Includes PR1 and PR2.

Demonstrable outcome:

- Open the new production workspace.
- Select an original video.
- Draw and edit a polygon directly over the image.
- Verify the polygon on three frames.
- Refresh and restore the draft.

Go/no-go gate:

- Coordinate tests pass at supported resolutions and aspect ratios.
- Three-frame gating is reliable.
- Existing Baseline and Broadcast routes remain unaffected.

### Milestone B: Functional Beta

Includes PR3, PR4A, and PR4.

Demonstrable outcome:

- Complete trial and tuning loops.
- Confirm a derived configuration.
- Run full tracking.
- Complete optional review and recomputation.
- Render and preview the final product.
- Resume every long-running state after refresh.

Go/no-go gate:

- Both review and no-review browser paths pass.
- No duplicate API mutations occur during rerender or refresh.
- A real short-video workflow produces a playable product and readable quality report.

### Milestone C: Release Candidate

Includes PR5 and PR6.

Demonstrable outcome:

- Navigate only between Production and Production History.
- Reopen a production by AssetGroup.
- Inspect the complete run lineage.
- Preview and download historical products.
- Open legacy URLs without losing context.

Go/no-go gate:

- All CI gates pass.
- Full-video validation passes on the selected release candidate.
- Accessibility, responsive, localization, and rollback checks pass.

---

## PR1: Production Workflow Foundation

**Branch:** codex/production-workflow-foundation

### Scope

- Add the /production route without replacing the existing main navigation yet.
- Define a pure production-workflow state model.
- Define prerequisite and transition guards.
- Define upstream-edit invalidation behavior.
- Add a versioned production-draft schema.
- Persist the draft locally and safely recover it.
- Bind every draft to path, size_bytes, and modified_at from the selected input.
- Add the production workspace shell.
- Show only the current step plus collapsed completed summaries.
- Add Back, Save and exit, and Next actions.
- Add Start new production, completed-draft archival/clear behavior, and confirmation before replacing an unfinished draft.
- Define how opening another production from history handles an existing unfinished draft.
- Introduce the frontend unit/component test runner needed by later PRs.
- Introduce Playwright Chromium test infrastructure with API route mocking.
- Add accessibility scanning support for new flows.
- Add the initial CI jobs and aggregate test:web, test:production-workflow, and test:e2e scripts in this PR; later PRs extend these jobs rather than deferring test-tool integration to cutover.

### Acceptance Criteria

- Next is disabled until a valid source is selected.
- Refresh restores a valid draft.
- Invalid or unknown draft versions fail safely to a recoverable state.
- Changing the source invalidates every downstream reference.
- Replacing a file at the same path with a different size or modified time invalidates calibration and downstream state.
- Starting or opening another production cannot silently overwrite an unfinished draft.
- Repeated rendering does not issue mutation requests.
- The route has loading, empty, error, and recovery states.
- The new route can ship without altering the legacy workflow.

### Required Tests

- Full state-transition table.
- Prerequisite-guard tests.
- Upstream invalidation tests.
- Draft serialize, restore, corrupt-data, and version-migration tests.
- Source-signature replacement tests.
- New-production and unfinished-draft replacement tests.
- Source selection component tests.
- Basic /production browser test.
- Chinese and English rendering smoke.
- Accessibility smoke with no critical or serious findings.

### Rollback Boundary

Removing the new hidden route and draft key returns the product to the existing UI. No backend or persisted run data changes.

## PR2: Interactive Field Calibration

**Branch:** codex/interactive-field-calibration

### Scope

- Add konva and react-konva using the repository supply-chain policy and frozen lockfile.
- Keep the existing preview image capability.
- Add a transparent Konva Stage overlay.
- Render an approved closed Line and draggable vertex Circles.
- Support add, drag, delete, undo, clear, and coordinate editing.
- Display point numbers and source-pixel coordinates.
- Render suggested and approved polygons with distinct styles.
- Display suggestion source and confidence.
- Integrate three-frame verification into the same calibration step.
- Display the same approved polygon on each confirmed frame.
- Store each confirmed frame together with the approved polygon digest used for that confirmation.
- Add a coordinate-list fallback for keyboard and assistive-technology users.
- Lazy-load the editor so Konva does not inflate the application entry bundle.

### Acceptance Criteria

- Operators can create and edit a valid polygon without editing JSON.
- A suggestion never becomes approved without an explicit operator action.
- Suggestions and approved coordinates are both visible.
- Three distinct actual frames using one source resolution are required.
- Confirm frame is disabled until the preview is loaded, the polygon is valid, and its overlay is visible.
- Editing the polygon clears previous frame confirmations.
- Changing the source clears calibration.
- Pointer interaction remains correct with scaling, letterboxing, device-pixel ratio, and mobile layout.
- Invalid coordinates are explained and never silently clamped.

### Required Tests

- Source/display coordinate conversion.
- Letterbox and aspect-ratio mapping.
- Drag clamping and handle selection.
- Minimum-point, boundary, finite-number, and non-zero-area validation.
- Suggestion/approval separation.
- Three distinct frames and matching-resolution validation.
- Rejection of frame confirmation before a valid visible polygon exists.
- Per-frame polygon-digest binding.
- Polygon edit invalidation.
- Playwright click, drag, undo, clear, and frame-navigation flows.
- Visual snapshots at desktop and mobile widths.
- Keyboard coordinate-editing flow.

### Quality Threshold

- Geometry and calibration pure modules must reach at least 90 percent branch coverage.
- Coordinate round-trip error must not exceed one display pixel.
- No critical or serious accessibility findings.

### Rollback Boundary

The editor is isolated behind the new production route. Legacy JSON/text calibration remains available until cutover.

## PR3: Trial, Tuning, and Configuration Freeze

**Branch:** codex/trial-tuning-and-config-freeze

### Scope

- Move existing Test Baseline behavior into the production workspace.
- Build trial requests from source, approved calibration, exclusions, frame range, and tuning state.
- Display queued/running progress, cancellation, success, failure, and retry.
- Display trial artifacts and the quality signals required for operator acceptance.
- Keep tuning and rerun inside one user-facing stage.
- Record multiple trial run IDs without overwriting prior attempts; each retry points to the preceding trial through parent_run_id.
- Require successful completion, readable required artifacts/quality signals, visible evidence, and an explicit operator Accept this trial action.
- Use the existing derive-config endpoint with a UUID-based output name and workflow metadata.
- Compute the returned configuration-text SHA-256 and store the configuration name, digest, and workflow lineage in the production draft.
- Store the expected configuration digest and workflow ID in a machine-readable full-run note when PR4 starts the full broadcast.
- Treat the snapshot as operator-locked and tamper-detectable, not server-enforced immutable.
- Include input_video in the derived configuration patch so it remains associated with the correct AssetGroup.
- Require explicit invalidation before editing an accepted upstream field.

### Acceptance Criteria

- Calibration must be complete before trial.
- The trial request exactly reflects approved calibration and tuning.
- Failed and cancelled trials cannot be confirmed.
- A rerun creates a separate run.
- A merely completed run with missing required result artifacts cannot be accepted.
- Confirmation produces a uniquely named persisted configuration snapshot.
- The confirmed configuration summary identifies its base config, patch, calibration, accepted trial, source signature, workflow ID, and digest.
- Same-name derive attempts are avoided by construction; a missing, modified, or digest-mismatched configuration blocks full-run submission and offers explicit re-confirmation.
- Refresh restores a queued/running/completed trial without creating another.
- Double-clicking or rerendering cannot create duplicate runs.
- If another backend run is active, the operator sees that task and a link to it; a start conflict does not lose the draft.

### Required Tests

- Exact request-payload tests.
- Queued, running, completed, failed, and cancelled states.
- Cancellation and retry.
- Multiple trial lineage.
- Configuration derivation and confirmation.
- Missing trial artifacts and explicit acceptance.
- Unique snapshot naming, deleted snapshot, modified snapshot, and digest mismatch.
- Single-active-run conflict.
- Upstream invalidation after confirmation.
- Refresh recovery.
- Mocked browser journey from calibrated source through accepted trial.

### Rollback Boundary

The legacy /baseline route remains functional. New trial orchestration affects only /production.

## PR4A: Review Evidence Provisioning

**Branch:** codex/review-evidence-provisioning

### Scope

- Reuse the existing `candidate_dataset`, `candidate_annotations`, `candidate_classifier`, `selective_policy`, and `selective_review` implementations as an offline evidence producer. PR4A does not reimplement an in-app multi-reviewer or training platform and does not rewrite those algorithms without a separately reviewed defect.
- Add a builder/validator for a self-contained `broadcast_review_evidence_bundle`. The bundle contains three mutually exclusive evidence packages plus the target queue and a complete file inventory:
  - model-development: train, temperature-calibration, and held-out test datasets plus annotation resolution, model weights, model manifest, and training report;
  - policy-qualification: previously unseen policy-calibration and policy-audit datasets, predictions, human truth, role assignment, qualification report, and frozen qualified policy; and
  - target-application: only the target run source, byte-identical tracking contract, application dataset/samples, predictions from the frozen model, decisions from the frozen policy, review timing, and queue.
- Add the proven integration fix that validates the model and policy packages independently, then applies the frozen policy to a separate application package. Queue bindings must distinguish qualification evidence from application dataset/predictions/decisions; they may not reuse one ambiguous `dataset` binding for all three populations.
- Require every bundle path and queue binding to be relative to and contained by the bundle root; reject absolute paths, `..`, links/reparse points, missing files, hash changes, and incomplete lineage. Build against the bundled source from the beginning instead of copying path-bound offline artifacts after generation.
- Add an explicit, restartable import/adoption job for a completed full broadcast run while keeping the existing review consumer authoritative.
- Accept only a server-managed inbox `bundle_id` plus `bundle_manifest_sha256`; never accept arbitrary client filesystem paths.
- Copy to a parent-run staging directory, rehash the entire inventory, verify exact source/contract/action-signal/profile/max-window compatibility and policy qualification, then rename the immutable generation into place. Write and validate its activation manifest before publishing the fixed root `selective_review_queue.v1.json` with exclusive create as the only commit marker.
- Require `coverage_complete=true`, `requires_additional_round=false`, no dropped candidate IDs, and at most 30 review windows. PR4A does not add multi-round review; an over-capacity or partial queue remains blocked.
- Surface compatible-bundle availability, import progress, blockers, capacity estimate, cancellation, retry, generation lineage, and queue readiness in legacy Broadcast through a reusable review-evidence hook/component. PR4 mounts the same unit in Production when the full-run stage lands; PR4A must not depend on unmerged PR4 code.
- Preserve the complete immutable evidence generation after activation; a retry or different bundle creates a new version and never rewrites accepted evidence.
- Offline producers deliver to the server-managed inbox by writing a hidden staging directory and atomically renaming it to the discoverable `bundle_id` only after the manifest and inventory are complete; discovery ignores hidden or incomplete directories.

### Annotation and Training Evidence Subproject

- This approved parallel subproject produces the first real self-contained bundle outside the HTTP service using the existing offline chain and the new bundle builder.
- Maintain three strictly separate populations: model-development train/temperature-calibration/test, previously unseen policy-calibration/policy-audit, and target application containing only the target run's 6,406 candidates.
- For globally reusable policy qualification, never use a target application candidate for model weights, temperature calibration, threshold selection, qualification audit, or another promotion decision. The approved PR4B exact-target scope may use a precommitted target sample only for qualification bound to that exact frozen target; those labels never change weights, calibration, thresholds, or any reusable/global policy. Repeated encodes, crops, resolutions, or temporal windows from the same match retain the same group identity.
- Each confirmed training truth requires two blind primary human votes from distinct reviewer identities and fingerprints; a primary reviewer cannot see a model prelabel or another primary vote before submission. Conflicts require an independent human adjudicator, and corrections are append-only superseding events.
- The subproject uses controlled work packets and separate ledgers; the current unauthenticated local frontend must not claim that it enforces multi-person identity or blindness.
- Runtime videos, tensors, ledgers, model weights, and bundles are excluded from Git. Git deliverables are only protocols, schemas, validators, deterministic fixtures, scripts, and runbooks.

### Minimum API and State Contract

- `GET /api/v1/runs/{run_id}/broadcast/review-evidence` returns authoritative state plus compatible server-managed bundle IDs without exposing local paths.
- `POST /api/v1/runs/{run_id}/broadcast/review-evidence/import` accepts `{bundle_id, bundle_manifest_sha256}` plus an optional explicit terminal `retry_from_job_id`, returns 202 for a new child job, and is content-idempotent. Cancellation reuses the existing run-cancel endpoint.
- Parent review-evidence lifecycle: `not_available`, `available`, `queued`, `copying`, `validating`, `committing`, `ready`, `failed`, `cancelled`, or `blocked`; the parent broadcast status remains `needs_review` throughout provisioning.
- The import child uses source `broadcast_review_evidence_import` and the existing queued/running/completed/failed/cancelled run status. It does not overload `broadcast.last_operation`, whose contract remains limited to recompute/render.
- `request_sha256` binds parent run ID, root contract SHA-256, source SHA-256, action-signal binding SHA-256, max windows, bundle manifest SHA-256, and provisioner version. The generation ID derives from that content identity.
- The same request digest returns the same job/result in every state. Only an explicit retry of the same blocked/failed/cancelled job may create a new child with `retry_of` lineage; a different bundle while another import owns the target returns conflict. A fixed queue or existing review decisions prohibit in-place replacement.
- Import occupies the backend single-active-work slot. Cancellation is cooperative before commit and rejected once the commit phase starts.
- A crash before the root queue commit marker leaves no consumable queue; a crash after it reconciles child and parent state from the validated generation manifest on startup.

### Qualification and Safety Gates

- No synthetic truth, empty-queue synthesis, default acceptance, or model-only confirmation.
- The annotation protocol, taxonomy, sampling plan, reviewer-role assignments, minimum evidence rules, evaluation split, metrics, and promotion thresholds are frozen and hashed before the final audit cohort is opened.
- Any protocol or threshold change creates a new attempt and requires a fresh, previously unobserved audit cohort. Audit results cannot be used to tune the attempt they evaluate.
- The existing `SelectivePolicyConfig` production safety floor cannot be silently lowered: accept precision target at least 0.98, minimum audit accepted count 100, minimum audit true-ball count 300, at least 3 independent evidence components, and existing per-cluster minimums. A stronger reviewed protocol may raise these gates.
- Classifier training requires at least three genuinely independent source groups and leakage-free train/calibration/test assignments. Every required evaluation split must contain human-confirmed match-ball and noise support. If the local evidence inventory cannot meet this, the attempt remains blocked; clips or encodes from one match cannot be declared independent matches.
- The current three local MP4 files represent one logical match group after grouping alternate encodes and derived clips, so they cannot qualify a new classifier by themselves.
- The existing policy inferential-unit contract must pass a separate feasibility review against the frozen evidence inventory before any real promotion. Its safety floor and provenance grouping cannot be weakened inside PR4A merely to make the target pass. If no independently qualified external bundle is supplied and the contract proves infeasible at available scale, the real run remains blocked from production activation pending a separately approved policy-statistics fix or explicit exhaustive-human mode.
- Model and policy promotion requires a held-out report with per-class support, precision/recall, calibration/selective-risk evidence, and explicit pass/fail reasons.
- The queue publisher verifies target run ID, source digest, tracking-contract digest, dataset digest, model digest, policy digest, feature schema, candidate coverage, and human-evidence lineage.
- A second reviewer can audit any published queue item back to the source frame, candidate, model output, policy role, and annotation history.
- Offline extraction/training records deterministic seeds, pinned dependencies, progress, and terminal reports. The online import is content-idempotent and recovers safely after refresh or process restart.
- Before build or import, calculate peak required space as existing retained generations plus all three evidence populations, maximum concurrent staging copy, checkpoints/model, review media/cache, and safety margin. Reject insufficient capacity without partial publication, enforce per-attempt quota, clean cancellable staging, and record retention deadlines. The current 6,406 application candidates alone require about 1.83 GiB of raw tight/context tensors before other populations and overhead; real import preflight must reserve substantially more than 2 GiB.

### Operator UI Requirements

- When the review queue is missing, query the shared review-evidence state instead of presenting an empty review or continuing automatically.
- If a compatible bundle is available, show its manifest identity and an explicit “Prepare review evidence” action. Rendering must never start the import implicitly.
- Show queued/copying/validating/committing progress, safe cancellation availability, exact blocker codes and recovery actions, retry state, generation identity, and capacity failures.
- When ready, invalidate run/artifact/review queries and enter the existing Broadcast review component without changing its review contract.
- Keep import state/mutations in a reusable controller used by legacy `/broadcast` and covered in an isolated Production host test; PR4 later mounts it in the real Production full-run stage. Include refresh recovery, StrictMode, and double-click protection.
- Provide Chinese and English copy, complete keyboard navigation, announced progress/errors, and a usable desktop and narrow-screen layout.

### Acceptance Criteria

- The deterministic multi-video fixture completes the offline chain and self-contained bundle build; the public service then discovers, imports, validates, relocates, atomically activates, and reconciles that bundle without trusting producer paths.
- Every negative prerequisite fails closed with a stable blocker code and recovery action; no fixed queue facade changes on failure or cancellation.
- Target application candidates are proven disjoint from all training and promotion evidence in manifests and validation reports.
- The queue's application dataset contains exactly the target source required by broadcast orchestration, while model-development and policy-qualification evidence remain separately bound and hash-validated.
- Applying a frozen policy to the target population cannot mutate or recompute the model-development or policy-qualification packages.
- Published queue counts reconcile exactly across target contract candidates, automatic decisions, abstentions, review items, and exclusions.
- The existing review consumer loads the activated queue without bypasses and rejects tampered, stale, cross-run, or unqualified artifacts.
- Legacy Broadcast can discover and explicitly import a compatible bundle, survive refresh, cancel before commit, and display authoritative ready/failure state without duplicate mutations; the reusable unit passes its isolated Production-host contract for PR4 integration.
- Activating or revoking an evidence generation changes only review-evidence state and related blockers; it preserves terminal-tail acknowledgement, every unrelated blocker, and all existing limitations.
- Real videos, candidate tensors, reviewer ledgers containing sensitive identities, and model weights are runtime artifacts and are not committed to Git.
- PR4A's engineering merge gate is satisfied when the implementation and deterministic validation pass, even if external evidence resources are still missing. This does not resume the real run toward production-ready/render.
- PR4's fail-closed engineering code may likewise be reviewed and merged after all managed-PR engineering gates pass. Operational production activation resumes only after the target run has an activated qualified queue, the existing GET review contract validates it, all summary counts reconcile, and the real evidence record receives an independent visual audit recording auditor identity, timestamp, generation ID, queue SHA-256, and reviewed media. The auditor cannot be the generation activator.

### Required Tests

- Preserve existing dataset, annotation, classifier, policy, and queue suites, including blind-vote identity, append-only resolution, leakage, qualification, lineage, and tamper tests.
- Bundle builder/validator tests cover every required artifact and binding, contained relative paths, relocation, deterministic inventory, source/contract/action compatibility, qualification, complete coverage, no additional round, and the 30-window cap.
- Import tests cover repeated/concurrent identical requests, different-bundle conflicts, ready replay, failed retry, single-active-work behavior, and exact parent/child lineage.
- Security tests cover absolute or parent paths, symlinks/reparse points, cross-run access, inbox TOCTOU, source mutation during copy, oversized inventory, hash/size mismatch, and insufficient disk.
- Recovery tests inject interruption during copy, validation, generation rename, root queue publication, and registry update; cancellation is covered at every pre-commit stage and rejected after commit starts.
- Production-consumer tests reject partial/additional-round queues and prove the activated root queue passes the existing review-window/action/recompute lineage validators.
- Legacy Broadcast and reusable Production-host UI tests cover no bundle, compatible bundle, explicit import, all progress states, double-click/StrictMode idempotency, cancellation, failure/retry, ready transition, refresh recovery, i18n, keyboard use, and accessibility.
- A small deterministic multi-video end-to-end fixture spans model-development, separate policy-qualification, target application, self-contained bundle, activated queue, existing review consumer, and recompute lineage without storing real media or model output in Git; new frontend evidence modules are included in the existing 90% branch/function/line/statement coverage gate, and new Python modules meet the repository's enforced changed-code coverage gate.
- One real-run evidence exercise against `production_full_0276b82e-3dc5-445e-8758-e2de527ea216`; record actual annotation count, dataset/model/policy/queue digests, qualification result, and independent visual audit. If the evidence cannot meet the approved thresholds, PR4A reports the measured blocker and must not publish a qualified queue.

### Deliverables

- Versioned evidence API/OpenAPI contract and generated clients.
- Self-contained evidence-bundle schema, builder, validator, inventory, relocation, atomic activation, and restart reconciliation.
- Legacy Broadcast evidence-import controls plus the reusable hook/component and Production-host contract consumed by PR4.
- Deterministic multi-video fixture covering the existing dataset, annotation, training, evaluation, model, policy, queue, bundle, and import chain.
- Frozen annotation/qualification protocol, evidence-inventory report, capacity/retention policy, and explicit resource-gap report.
- Offline producer runbook covering controlled blind work packets, adjudication ledgers, reproducible training/evaluation, bundle construction, import, retry, rollback, audit, and incident response.
- Real-run evidence record when a genuinely qualified bundle becomes available; until then the record must state the exact resource blocker and PR4 operational production activation remains paused.

### Rollback Boundary

PR4A is additive, but disabling its UI/API does not remove an already activated fixed queue facade. Rollback first prevents new attempts and activations, then handles the active generation as follows:

- Activation, review submission, and revocation share one parent-run mutation lock. The consumer accepts the root queue commit marker only when its generation activation manifest and every bound hash validate; an existing different qualified generation returns conflict and is never overwritten.
- Before any review decision or downstream trajectory generation consumes the queue, revoke only when the activation manifest and exact queue SHA-256 still match; preserve all immutable source evidence and audit artifacts.
- After any consumption, never delete or replace the evidence in place. Create a new generation or enter the incident-response flow with complete lineage.
- Incomplete staging directories may be cleaned according to the retention policy; qualified evidence, votes, activation reports, and consumed generations remain auditable.
- Activation or revocation refreshes the broadcast facade, registry generation, and query-visible state inside the same controlled mutation. Tests cover activation-versus-review and revocation-versus-review races, pre-consumption revocation, and post-consumption rollback refusal.

## PR4: Full Production, Review, and Render

**Branch:** codex/full-production-review-render

### Scope

- Reuse the existing broadcast workflow state and recovery helpers.
- Remove duplicate source and calibration input from the new production path.
- Start full tracking from the confirmed configuration snapshot.
- Set full broadcast parent_run_id to the accepted trial run ID.
- Force full-run invariants regardless of trial settings: start_frame=0, max_frames=null/full source, output.save_tracking_contract=true, enable_follow_cam=false, and the confirmed calibration_confirmation.
- Exclude trial-only frame range, temporary output directory, and legacy trial render options.
- Show full-run progress, cancellation, failure, and recovery.
- Reuse the existing review component for required review windows.
- Support no-review, review, recomputing, and trajectory-ready paths.
- Reuse the verified render and delivery behavior.
- Persist parent run identity in both the URL and production draft.
- Show a completed product view in the production workspace.
- Show playable video, quality state, limitations, and verified downloads.

### Required State Matrix

- tracking
- needs_review
- recomputing
- trajectory_ready
- rendering
- ready
- failed
- cancelled
- zero-candidate review
- populated review
- completed child operation with delayed parent update
- page refresh during every active state

### Acceptance Criteria

- Full tracking always uses the confirmed snapshot.
- Full tracking rechecks the stored configuration digest before submission.
- Full tracking request cannot inherit trial-only range or output settings.
- A competing active backend task is shown as a recoverable blocker.
- Manual review appears only when required.
- Review and render submissions cannot be duplicated.
- Refresh resolves the correct parent and active child.
- Ready without broadcast.mp4 is a blocked delivery.
- Artifact URLs must pass existing verification before display.
- Product downloads match the authoritative parent generation.

### Required Tests

- Extend the existing broadcast workflow and component suites.
- Production-to-broadcast adapter tests.
- Full-run invariant and trial-option exclusion tests.
- Accepted-trial parent lineage and configuration-digest verification.
- Single-active-run conflict and retry tests.
- Review and no-review browser journeys.
- Refresh and recovery at every active state.
- Failed and cancelled run recovery.
- Ready-without-media negative test.
- Final product preview and download-link verification.

### Real-Video Gate

- Run one short real trial/full/render workflow.
- Verify that broadcast.mp4 is playable.
- Verify the quality report can be loaded.
- Record the source, configuration snapshot and digest, run IDs, artifacts, visual inspection result, and outcome.

### Rollback Boundary

The existing /broadcast route remains available. The new path is an orchestration layer over existing APIs.

## PR5: Grouped Production History

**Branch:** codex/grouped-production-history

### Scope

- Replace the flat history projection with AssetGroup-based groups.
- Use input_video.path as the normal client key and treat group_id only as a collision-checked alias.
- Preserve search, status filters, cancellation, and output deletion.
- Summarize trial count, active work, full-run status, and product availability.
- Build a deterministic group timeline from parent_run_id, source, broadcast operation metadata, and metadata.production_workflow; time is a display/sort field, never a guessed lineage edge.
- Add a group-detail view.
- Add product preview, quality summary, limitations, and downloads.
- Support multiple historical product versions.
- Treat ready broadcasts as unverified product candidates in the group list; verify products lazily in detail through the dedicated artifacts endpoint and cache by run_id plus status_generation.
- Avoid per-row artifact requests and any 1,000-run N+1 query pattern.
- Keep failed and cancelled attempts visible but distinct from products.
- Put unbound legacy records in an explicit fallback group.
- Cancel the active broadcast operation selected by broadcastCancellationTarget rather than always cancelling its parent.
- Disable parent deletion while children exist with an explanation, or require explicit child-first deletion.
- Invalidate AssetGroup, run, artifact, and product-verification caches after cancel or delete.

### Acceptance Criteria

- A run appears in exactly one AssetGroup.
- Parent/child operations appear in the correct lineage.
- Trial retry, accepted trial, full broadcast, recompute, and render lineage can be rebuilt without relying on timestamp proximity.
- Group summaries distinguish unverified ready candidates from artifact-verified products.
- Product classification requires ready plus broadcast.mp4 verified through the dedicated endpoint.
- Failed trials never appear as products.
- Multiple products show their creation time and configuration identity.
- Active progress continues to refresh.
- Deleting or cancelling a run updates the group summary safely.
- History remains usable with at least 1,000 fixture runs.

### Required Tests

- AssetGroup projection.
- Timeline ordering and parent/child linking.
- Accepted-middle-trial and multiple-full-product lineage.
- Duplicate group_id aliases with distinct input paths.
- Product classification positive and negative cases.
- Lazy artifact verification, status-generation cache invalidation, and no-list-page-N+1 behavior.
- Multiple trials and products.
- Failed, cancelled, and unbound legacy records.
- Search and filtering.
- Cancellation and deletion confirmations.
- Child-target cancellation and child-first deletion behavior.
- Group-detail and product-preview browser tests.
- Large fixture interaction check.

### Quality Threshold

- History projection and product-classification pure modules must reach at least 90 percent branch coverage.
- No run may appear in multiple groups.
- The browser journey must produce no console error or unhandled promise rejection.

### Rollback Boundary

The original flat-history component remains available until final cutover.

## PR6: Cutover, Compatibility, and Release Hardening

**Branch:** codex/production-workflow-cutover

### Scope

- Make Production and Production History the only primary navigation items.
- Route the default entry to the production workspace.
- Preserve legacy deep links:
  - /baseline routes to the earliest valid production step and shows a migration notice; it may enter trial only when source and calibration prerequisites exist.
  - /broadcast?run=... routes to the corresponding production run.
  - Existing delivery links route to the relevant history/product detail.
- Keep /ai and /deliverable as unlisted compatibility routes for at least one release; expose relevant AI and highlight actions from contextual advanced actions in Production or History before any later removal.
- Keep legacy implementation components for at least one release cycle.
- Complete Chinese and English copy.
- Complete light/dark and responsive checks.
- Complete keyboard, focus, status-announcement, and contrast work.
- Enforce lazy loading for the calibration editor.
- Make the PR1 aggregate web, coverage, accessibility, and E2E jobs final required cutover checks and extend them with the complete journeys.
- Update operator and testing documentation.
- Produce release evidence and rollback instructions.

### Acceptance Criteria

- Main navigation contains only the two agreed primary workspaces.
- Legacy links do not return a 404 and preserve run context.
- The five-stage workflow is complete without using legacy pages.
- The mobile layout can finish the core workflow.
- No critical or serious accessibility finding remains.
- No browser console errors occur in critical journeys.
- Konva is not part of the initial application chunk.
- Existing backend and generated-client checks remain green.

### Required Tests

- Legacy URL mapping.
- Complete happy path without review.
- Complete happy path with review.
- Trial failure, tuning, and retry.
- Invalid calibration recovery.
- Refresh during long-running states.
- Product completion and historical reopening.
- Chinese/English smoke.
- Desktop/mobile visual snapshots.
- Accessibility checks.
- Bundle/chunk inspection.

### Release Validation

- Run the official project test route.
- Run the official full-video validation route against the selected release candidate.
- Validate one known real match from source through final media.
- Record exact run IDs, configuration snapshot, quality result, artifacts, limitations, and operator decision.

### Rollback Boundary

Because existing backend contracts remain compatible, the PR4A contract is additive and independently disableable, and legacy routes are retained, release rollback remains a frontend navigation/routing revert plus prevention of new evidence activations. Already consumed evidence follows PR4A's generation-safe rollback rules. Legacy code is removed only in a later explicitly approved cleanup.

---

## Test and Quality Program

### Updated CI Requirements

PR1 first installs and activates the new browser, accessibility, and coverage infrastructure in CI. PR1 and every later PR must then pass:

1. Frozen dependency installation under the repository minimum-release-age policy.
2. Generated API-client consistency.
3. Every existing frontend unit and component test.
4. New production workflow, geometry, evidence-provisioning, annotation, training-orchestration, and history tests.
5. TypeScript type checking.
6. Production web build.
7. Playwright Chromium critical-path tests.
8. Accessibility scan for new primary views.
9. The official Python verification route.
10. A clean diff check for unexpected generated files.
11. Coverage enforcement for the new pure workflow, geometry, evidence-provisioning, and history modules.
12. A deterministic small multi-video evidence fixture covering dataset through activated review queue.
13. Security and recovery checks for path isolation, request limits, concurrent attempts, atomic activation, restart reconciliation, and staging cleanup.
14. A repository check that rejects committed real videos, candidate tensors, annotation ledgers, and model weights.

The web package should expose aggregate scripts equivalent to:

- test:web
- test:production-workflow
- test:e2e

The aggregate web test must include existing lifecycle, AI improvement status, broadcast workflow, and broadcast component tests. No existing suite may disappear merely because the navigation changes.

### Required Automated Browser Journeys

1. Happy path with no manual review.
2. Happy path with required manual review.
3. Invalid polygon, correction, and successful three-frame confirmation.
4. Rejection of three-frame confirmation before a valid visible polygon.
5. Same-path source replacement invalidates the draft.
6. Trial failure, parameter adjustment, and successful rerun.
7. Missing trial artifacts block acceptance.
8. Configuration confirmation followed by an attempted upstream edit.
9. Modified or deleted locked configuration blocks full submission.
10. Another backend task occupies the single active-run slot.
11. Refresh during trial.
12. Refresh during full tracking.
13. Refresh during review/recomputation.
14. Refresh during rendering.
15. Cancel and create a new retry from the same locked configuration.
16. Completed product in the workspace.
17. Reopen and lazily verify a completed product from grouped history.
18. Cancel an active broadcast child and enforce child-first deletion.
19. Start a new production while an unfinished draft exists.
20. Legacy Baseline and Broadcast URL migration.

### Accessibility Requirements

- No critical or serious automated accessibility findings.
- Canvas has an equivalent coordinate-list editor.
- Every primary action has an accessible name.
- Progress and completion changes use appropriate live status announcements.
- Keyboard focus remains visible and follows workflow order.
- Error messages identify the affected field or step.
- Color is not the sole distinction between suggested and approved polygons.

### Visual and Responsive Requirements

At minimum validate:

- 1440 by 900 desktop
- 1280 by 720 desktop
- 390 by 844 mobile
- light theme
- dark theme
- Chinese
- English

Calibration snapshots must cover the suggested, editing, confirmed, and invalid states. History snapshots must cover active, completed, failed, empty, and large-list states.

### Performance Requirements

- React Konva is loaded only when calibration is active.
- Polling is disabled when a view no longer needs it.
- Mutation buttons are disabled while their request is pending.
- No duplicate mutation is caused by Strict Mode, rerender, refresh, or double-click.
- History projection is memoized and remains interactive with 1,000 fixture runs without eagerly fetching artifacts for every ready run.
- No new application-entry bundle regression is accepted without review and recorded justification.

### Real-Media Quality Requirements

CI uses mocked API responses and small deterministic fixtures. Real-media validation is a release gate rather than a per-PR CI gate.

Before release:

- Complete one short real-video workflow after PR4.
- Complete one known full-video workflow after PR6.
- Run the existing validate:full-video command.
- Confirm final media decodes and plays.
- Record the operator's visual acceptance of the trial and final product.
- Confirm source/config/trajectory/render lineage.
- Recompute and compare the stored configuration digest.
- Confirm the quality report and any declared limitations.
- Confirm all advertised downloads resolve.

---

## Final User Acceptance Checklist

- Only Production and Production History appear as primary navigation.
- The workspace highlights only the current step.
- The operator can draw and edit the field polygon on the image.
- Suggested and approved polygons are visibly different.
- Point coordinates are visible in source pixels.
- Three-frame confirmation is part of field calibration.
- Trial and tuning form a clear retry loop.
- A successful trial with readable evidence and explicit operator acceptance is required before configuration confirmation.
- The confirmed configuration is uniquely named, operator-locked, digest-verified, and traceable; later modification or deletion is detected.
- Active full tracking can be reopened after page refresh and monitoring continues.
- Cancelled or failed work can create a new retry from the same locked configuration; the cancelled computation itself is not resumed.
- A competing active backend task is shown as a recoverable blocker.
- Manual review appears only when needed.
- Render and quality status are recoverable after refresh.
- A product requires ready state and broadcast.mp4 verified through the dedicated artifacts endpoint.
- Final media and evidence downloads work.
- History is projected from AssetGroup and keyed by the original input path rather than a collision-prone slug.
- Each group's trial-to-configuration-to-full-run lineage is visible from persisted relationships and workflow metadata.
- Failed attempts never appear as final products.
- Existing runs and artifacts remain accessible.
- Legacy links remain usable during migration.

## Managed PR Execution Rules

For each PR:

1. Inspect repository instructions and current status.
2. Identify and preserve unrelated local changes.
3. Update local main from origin/main.
4. Create the declared fresh branch.
5. Assign a dedicated implementation agent with file ownership and acceptance criteria.
6. Require tests first for behavior changes.
7. Run a separate specification-compliance review.
8. Resolve every valid specification gap.
9. Run a separate code-quality review.
10. Resolve Critical and Important findings.
11. Run focused and required validation.
12. Commit intentionally and push.
13. Create the GitHub PR and record the feedback-window start.
14. Wait at least ten full minutes after the latest material push.
15. Fetch checks, reviews, inline comments, issue comments, and automated-review comments.
16. Classify each actionable comment as valid, invalid, or ambiguous with rationale.
17. Fix confirmed valid issues and repeat validation.
18. Restart the ten-minute window after any material follow-up push.
19. Fetch remote feedback once more immediately before merge.
20. Merge only when checks pass and no valid or ambiguous issue remains.
21. Delete branches only when explicitly authorized.
22. Update main before starting the next PR.

During active execution, report at least every fifteen minutes:

- Current PR and branch
- Current status
- Checks
- Blockers
- Next action

## Final Program Completion Evidence

The final handoff must include:

- All eight PR URLs
- Branch and commit for each PR
- Checks executed and their results
- Specification-review findings and resolutions
- Code-quality findings and resolutions
- Remote-comment classification log
- Merge status
- Authorized branch-cleanup status
- Final real-video validation record
- Known limitations
- Rollback instructions
- Remaining follow-up work, if any

## Independent Review Resolution

An independent review agent inspected the original plan against the current routes, API service, generated client, broadcast helpers, tests, and CI. The review concluded that the original six-PR sequence was sound but required revision before execution. No reviewer edits were applied directly; the controller evaluated and incorporated the findings below. PR4A is a later approved scope expansion prompted by the real-video gate and is subject to a fresh independent specification review.

Accepted Critical resolutions:

1. Replaced the unsupported claim of server-enforced immutable derived configurations with uniquely named, operator-locked, digest-verified configuration snapshots.
2. Added persistent trial-to-configuration-to-full-run lineage through parent_run_id, workflow metadata, and machine-readable full-run notes.

Accepted Important resolutions:

1. Drafts now bind path, size_bytes, and modified_at so same-path source replacement invalidates calibration.
2. AssetGroup group_id is no longer treated as globally unique; input_video.path is the normal client key.
3. Ready-product verification is lazy through the dedicated artifacts endpoint and cached by run_id plus status_generation, avoiding list-page N+1 requests.
4. Trial-only frame range and output settings are explicitly removed from full broadcast requests.
5. Playwright, accessibility, coverage, aggregate scripts, and initial CI integration belong to PR1; PR6 only finalizes cutover gates.
6. The backend single-active-run limitation is part of workspace behavior and automated tests.
7. Refresh recovery is distinguished from cancellation/failure retry; cancelled work is never described as resumable.
8. History cancellation targets the active broadcast operation and deletion respects child-first constraints.
9. Trial acceptance requires readable evidence and an explicit operator decision.
10. Start-new-production and unfinished-draft replacement behavior is specified.
11. Three-frame confirmation is bound to a valid visible polygon and its digest.
12. Legacy AI, Deliverable, and Highlight capabilities remain available through unlisted compatibility routes and contextual advanced actions for at least one release.

Review conclusion after these resolutions: **executable under the managed multi-PR workflow**, subject to isolating the existing unrelated pnpm-workspace.yaml modification before PR1.
